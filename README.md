# Usage
Install dependencies with:
```shell
yarn
```
or
```shell
npm install
```
------
Run server with command at root of project
```shell
yarn dev:server
```
or
```shell
npm run dev:server
```
------
Server runs on `localhost:3000`
